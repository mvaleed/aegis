---
apiVersion: v1
kind: Service
metadata:
  name: aegis-service
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: grpc
      protocol: TCP
      port: 9090
      targetPort: 9090
  selector:
    app: aegis-service
